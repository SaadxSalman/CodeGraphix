{
  "name": "vectorphile-site",
  "version": "1.0.0",
  "description": "A static site with Cloudflare Pages Functions",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build steps needed, dependencies installed by Bun.'",
    "deploy": "npx wrangler pages deploy . --project-name=vectorphile-site"
  },
  "keywords": [
    "cloudflare",
    "pages",
    "middleware"
  ],
  "author": "saadsalmanakram",
  "license": "ISC",
  "dependencies": {
    // Add any necessary runtime dependencies for your functions here
  },
  "devDependencies": {
    "wrangler": "^3.0.0" 
    // It's good practice to keep wrangler listed here
  }
}